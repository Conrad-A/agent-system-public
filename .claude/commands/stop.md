---
description: Stop a worker; it files a PARTIAL report with the evidence so far — /stop <worker_id> [--kill]
allowed-tools: Bash(python3 plumbing/stop.py:*)
---
!`python3 plumbing/stop.py $ARGUMENTS`

Relay the output above verbatim. The PARTIAL report appears in /status.
