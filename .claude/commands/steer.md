---
description: Send a running worker a note it sees before its next model call — /steer <worker_id> <message>
allowed-tools: Bash(python3 plumbing/steer.py:*)
---
!`python3 plumbing/steer.py $ARGUMENTS`

Relay the output above verbatim.
