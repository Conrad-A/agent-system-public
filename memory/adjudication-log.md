# adjudication log — curation-gate rulings (SPEC §3d write contract)

One row per resolved conflict so the same conflict replays to the same
outcome. Format: date · conflict · ruling · reason.

(none yet)
